[alembic]
script_location = alembic
prepend_sys_path = src:.
version_path_separator = os

# Placeholder only. Runtime URL comes from get_settings().database_url
# (alembic/env.py) or is injected by run_migrations().
sqlalchemy.url = driver://user:pass@localhost/dbname

[post_write_hooks]
